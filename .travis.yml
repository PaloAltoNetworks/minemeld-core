language: python
python:
  - "2.7"

addons:
  apt:
    - libleveldb-dev
    - librrd-dev
    - libxslt1-dev

install:
  - pip install -r requirements.txt
  - pip install -r requirements-web.txt

script:
  - true
